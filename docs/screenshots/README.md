# Screenshot placeholders

After running SourceLens locally, add screenshots with these filenames:

- `collection-upload.png` — empty state and PDF upload sidebar
- `indexed-collection.png` — indexed-document summary in the sidebar
- `answer-with-sources.png` — assistant answer with expanded referenced passages

The root README already includes a placeholder table so the repository remains readable before screenshots are added.
